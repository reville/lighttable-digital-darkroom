# Development-only. Never installed into the application runtime; the app ships
# no machine-learning framework at all and reaches a converted model through
# Core ML in the Swift helper.
#
# These versions are pinned because the combination matters:
#   * numpy must be < 2. coremltools calls int() on one-element arrays, which
#     NumPy 2 rejects, and every conversion fails with a confusing
#     "only 0-dimensional arrays can be converted" error.
#   * torch and torchvision must match each other.
torch==2.7.0
torchvision==0.22.0
coremltools==9.0
numpy==1.26.4
einops==0.8.1
timm==1.0.29
# The pinned upstream network imports this profiling helper at module load,
# even though conversion never calls its command-line benchmark.
thop==0.1.1.post2209072238

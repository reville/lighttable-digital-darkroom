name: Python tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  unit:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install the runtime set
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-runtime.lock

      - name: Prepare the pinned film runtimes
        env:
          PYTHON_SOURCE_REV: 3bb2c2d2801ff68b92019cf1dbcbb133d60832bc
          RUST_SOURCE_REV: 9dd59b0380194b93686aaa230a8bb9680aa270a4
        run: |
          git clone --filter=blob:none --no-checkout https://github.com/andreavolpato/agx-emulsion.git "$RUNNER_TEMP/spektrafilm-python"
          git -C "$RUNNER_TEMP/spektrafilm-python" fetch --depth=1 origin "$PYTHON_SOURCE_REV"
          git -C "$RUNNER_TEMP/spektrafilm-python" checkout --detach "$PYTHON_SOURCE_REV"
          git clone --filter=blob:none --no-checkout https://github.com/turbasvin/spektrafilm-rs.git "$RUNNER_TEMP/spektrafilm-rust"
          git -C "$RUNNER_TEMP/spektrafilm-rust" fetch --depth=1 origin "$RUST_SOURCE_REV"
          git -C "$RUNNER_TEMP/spektrafilm-rust" checkout --detach "$RUST_SOURCE_REV"
          mkdir -p engine vendor/spektrafilm/src
          cp -R "$RUNNER_TEMP/spektrafilm-rust/data" engine/data
          cp -R "$RUNNER_TEMP/spektrafilm-python/src/spektrafilm" vendor/spektrafilm/src/spektrafilm

      # The engine-parity script needs the built Rust binary and profile data,
      # so it stays out of this job; `tests/engine_parity.py` is run from the
      # release script instead.
      - name: Unit and contract tests
        run: PYTHONPATH=".:vendor/spektrafilm/src" python -m unittest discover -s tests -p 'test_*.py' -t tests --verbose

  installers:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Installer behavior and launcher tests
        run: |
          python3 -m unittest discover -s tests -p test_installer_manifests.py -v
          python3 -m unittest discover -s tests -p test_cli_installation.py -v
          npm test --prefix packaging/npm
          (cd packaging/npm && npm pack --dry-run)

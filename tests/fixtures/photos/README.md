# Real-photo test fixtures

These are unmodified embedded JPEG previews extracted from three camera RAWs.
`provenance.json` records each original source, original hash, extracted hash,
and CC0 declaration. They are functional test inputs, not product endorsements.
The full RAW corpus is optional and does not ship with the application.
